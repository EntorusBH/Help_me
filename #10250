#include <stdio.h>

int main() {
	int t,h,w,n,a,b;
	scanf("%d \n", &t);
	for (int i=0;i<t;i++) {
		scanf("%d %d %d", &h, &w, &n);
		a=n%h;
		b=n/h;
		if (n%h==0) {
			a=h;
			printf("%d \n", a*100+b);
		}
		else printf("%d \n". a*100+b+1); //여기서 a변수 관련해서 계속 오류가 나는데 왜 나는지.. 
	}
} // #10250
