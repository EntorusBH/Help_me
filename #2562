#include <stdio.h>

int main() {
	int i,M,a,p;
	scanf("%d \n", &M);
	for (i=1;i<8;i++) { // 서로다른 9개의 수를 입력하는 문제에서. 이미 한 숫자를 입력해둔 상태에서. i=0;i<8이면 8개 입력받아야되는거 아닌가? 왜이러지..
		scanf("%d \n", &a);
		if (a>M) {
			M=a;
			p=i+1;
		}
	}
	printf("%d \n", M);
	printf("%d", p);
	return 0; //   #2562
}
